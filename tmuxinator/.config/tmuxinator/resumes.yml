# /Users/infowizard/.config/tmuxinator/resumes.yml

name: resume
root: "~/Repos/github.com/tittitoo/resume/"

windows:
  - resume:
      panes:
        - source .venv/bin/activate.fish; clear
  - files:
      panes:
        - source .venv/bin/activate.fish; clear
  - playground:
      # root: "~/.config/scripts"
      panes:
        - source .venv/bin/activate.fish; clear
